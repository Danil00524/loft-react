import React from 'react';
import { render } from '@testing-library/react';
import { shallow } from 'enzyme';

describe('header', () => {
    it('change state isLoginIn', () => {
        expect();
        // const wrapper = shallow(<Header />);
        // expect(wrapper.contains(<Header />)).toEqual(false);
    });
});
